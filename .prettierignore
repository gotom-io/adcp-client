**/*.generated.ts
schemas/cache/
compliance/cache/
docs/

# Workspace scratch directory — may contain checkouts of other repos with
# their own formatting conventions. Always gitignored; should not block CI.
.context/

# Vendored AdCP conformance vectors — byte-identical to upstream. Prettier
# would reformat and diverge from the source of truth. Remove once vectors
# load from compliance/cache/ (already ignored) instead of test/fixtures/.
test/fixtures/webhook-signing-vectors/

# Vendored AdCP 3.2 OAuth metadata-graph storyboard and vectors. Keep these
# byte-identical to the upstream compliance bundle.
test/fixtures/oauth-setup/

# AdCP 3.1 v2 projection fixtures — vendored from the spec repo's
# `static/examples/products/canonical/` + `server/src/creative-agent/`.
# Byte-identical to upstream so projection tests don't drift on local
# formatting changes.
test/lib/v2-projection-fixtures/
