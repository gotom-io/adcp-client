---
'@adcp/sdk': patch
---

Adopt the signed AdCP 3.2.0-beta.3 bundle, preserve inherited request fields in wire-safe fan-out helpers, validate the complete compact proposal and direct-buy lifecycle storyboards against the new protocol pin, and enforce AND-composed `requires_all_capabilities` storyboard gates.
