---
'@adcp/sdk': patch
---

Use an agent's discovered `covers_content_digest` policy to exclude only the mutually exclusive request-signing refusal vectors 007 and 018 when they do not apply. When running the 3.0 or 3.1 compliance line, capability blocks that omit the field now use the protocol's effective `either` default.
