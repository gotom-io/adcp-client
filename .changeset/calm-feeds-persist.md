---
'@adcp/sdk': minor
---

Add versioned persistence hooks to `WholesaleFeedSync` so mirrors can restore cached entities, feed versions, pricing versions, and webhook cursors across process restarts.
