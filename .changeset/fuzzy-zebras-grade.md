---
'@adcp/sdk': patch
---

Grade missing `parallel_dispatch_runner` contracts as canonically `not_applicable` while keeping required phases non-failing when they mix that skip with other unavailable test-kit contracts.
