---
'@adcp/sdk': minor
---

Add PostgreSQL and Redis webhook delivery stores plus durable, lease-fenced outbox recovery backends, secret-reference adapters, migrations, and a reusable recovery polling API.
